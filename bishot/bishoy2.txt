#include <iostream>
#include <string>
using namespace std;
struct Time {
    int hours;
    int minutes;
    int seconds;
};
struct movie {
    string name;
    Time duration;
    float price;
    int year;
    string language;
};
void inputmovie()
{
    movie m[100];
    char choose;
    cout << "do you want to enter movie:" << endl << "yes" << '/t' << "no";
    cin >> choose;
    while (choose == 'y') {
        int i = 0;
        cout << "Enter movie name : ";
        cin >> m[i].name;
        cout << "Enter movie in hours : ";
        cin >> m[i].duration.hours;
        cout << "Enter movie in minutes :";
        cin >> m[i].duration.minutes;
        cout << "Enter movie in seconds :";
        cin >> m[i].duration.seconds;
        cout << "Enter movie price : ";
        cin >> m[i].price;
        cout << "Enter movie year : ";
        cin >> m[i].year;
        cout << "Enter movie language : ";
        cin >> m[i].language;
        i++;
        cout << "added";
        cout << "do you want to enter movie:" << endl << "yes" << '/t' << "no";
        cin >> choose;
    }
}
struct MovieRental {
    string title;
    string customer;
    int rentalPeriod;
    float rentalFee;
};
const int MAX_MOVIES = 100;
MovieRental rentedMovies[MAX_MOVIES];


int main() {
    inputmovie();




    int numMoviesRented = 0;
    char add;
    cout << "do you want to add a film"<<endl<<"yes"<<'/t'<<"no";
    cin >> add;
    while (add == 'y') {
        int i = 0;
        cout << "Enter the movie title: ";
        getline(cin, rentedMovies[i].title);

        cout << "Enter the customer name: ";
        getline(cin, rentedMovies[i].customer);

        cout << "Enter the rental period (in days): ";
        cin >> rentedMovies[i].rentalPeriod;

        cout << "Enter the rental fee: ";
        cin >> rentedMovies[i].rentalFee;
        i++;
        numMoviesRented++;
        cout << "do you want to add a film" << endl << "yes" << '/t' << "no";
        cin >> add;
    }

    for (int i = 0; i < numMoviesRented; i++) {
        cout << "Movie #" << i + 1 << endl;
        cout << "Title: " << rentedMovies[i].title << endl;
        cout << "Customer: " << rentedMovies[i].customer << endl;
        cout << "Rental period: " << rentedMovies[i].rentalPeriod << " days" << endl;
        cout << "Rental fee: $" << rentedMovies[i].rentalFee << endl;
        cout << endl;
    }

}